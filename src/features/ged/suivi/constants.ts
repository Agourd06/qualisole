/** Max GEDs to load for the left (unassigned) column. */
export const GED_LIMIT = 500;

/** Items per page in the left column gallery. */
export const DEFAULT_PAGE_SIZE = 10;

/** Same as Constat: left = unassigned GEDs, right = assigned (folder/gedparallel). */
export const DROPPABLE_GEDS = 'geds';
export const DROPPABLE_RIGHT = 'assigned';
